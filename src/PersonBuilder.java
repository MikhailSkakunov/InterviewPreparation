public interface PersonBuilder {

    Person setName (String firstName);
    Person setLastName (String lastName);
    Person setMiddleName (String middleName);
    Person setCountry (String country);
    Person setAddress (String address);
    Person setPhone (String phone);
    Person setAge (int age);
    Person setGender (String gender);

}
